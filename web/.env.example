# Optional Responses-compatible provider configuration.
AI_BASE_URL=https://api.openai.com/v1
AI_TOKEN=your-token
AI_MODEL=gpt-5-mini

# SQLite database and screenshots directory. Defaults to ./data when unset.
DATA_DIR=./data
